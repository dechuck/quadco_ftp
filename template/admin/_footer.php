<?php

	/***********************
	** Build Sidebar Menu **
	***********************/
	
	// if( strpos($_SERVER['SCRIPT_FILENAME'], 'user.php') == false ) {
	
		// // Variables
		// $_ACTION = 'menu';
		// $_MENU   = array();

		// // Go into modules folder
		// $folder = $_CONFIG['PATH_ROOT'] . $_CONFIG['PATH_SUB'] . $_CONFIG['PATH_MODULES'];
		// $handle = opendir($folder);
		// while( $file = readdir($handle) ) {
		
			// if( $file != '.' && $file != '..' ) {
			
				// include($folder . $file);
				// admin_menuBuild($menu, $_MENU);
			
			// }
			
		// }
		
		// // Assign to template
		// admin_menuTemplate($_MENU);
		
		// unset($_ACTION);
	
	// }

?>